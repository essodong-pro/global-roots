# GlobalRoots Project Board Plan

Create the public GitHub Project Board and add the following issues. Replace the placeholder URLs
in `README.md` when the repository and board are available.

| Priority | Issue | Acceptance criteria |
| --- | --- | --- |
| P0 | Build responsive GlobalRoots shell | Home page works on mobile and desktop with accessible navigation. |
| P0 | Create country profile data model | Country records include region, summary, tags, and a stable identifier. |
| P0 | Build searchable country directory | Search and region filters update results without a page reload. |
| P0 | Add community story submission | Required fields validate and a successful submission gives clear feedback. |
| P1 | Add quiz question data and scoring | Users can select an answer and receive immediate correctness feedback. |
| P1 | Create country detail route | Selecting a country opens a page with etiquette, culture, and celebration details. |
| P1 | Add `/api/countries` endpoint | Endpoint returns the country collection as JSON. |
| P2 | Add `/api/stories` persistence | Valid stories can be created and retrieved from the application data store. |
| P2 | Add interactive cultural map | Selecting a region exposes the matching country highlights. |